import { createContext } from 'react'

/**
 * @import { ToolsContext } from './ToolsContext'
 */

const ToolsContext = createContext(
  /** @type { ToolsContext } */ ( /** @type { unknown } */ ( null ) ),
)

export { ToolsContext }
